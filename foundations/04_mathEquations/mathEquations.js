const a = 1 - 1 // Freebie!!! This is the answer to "one minus one"
const b = 1 + 8
const c = 22 * 3
const d = 5 % 4
const e = b - 17
const f = a + b + c + d + e // Correction here: I made this like "0 + 9 + 66 + 1 + -8" before but I should've used variables instead, both give the same result however sum of variables are makes more sense instead of sum of results because if a variable changes the f variable wouldn't work correctly

// Do not edit below this line
module.exports = {a, b, c, d, e, f}
